
public enum UIType
{
    Subitem = -1,
    Background,
    Auto,
    Fixed,
    Popup,
    Top = 1000,
}

public enum SoundType
{
    Bgm,
    Effect,
    UI,
}

public enum AddressableLabel
{
    Default,
    Game,
}

public enum SceneType
{
    Unknown,
    MainMenuScene,
    LoadingScene,
    VillageScene,
    DungeonScene,
}
